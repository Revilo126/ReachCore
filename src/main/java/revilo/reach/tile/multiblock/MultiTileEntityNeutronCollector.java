package revilo.reach.tile.multiblock;

/*
*/

public class MultiTileEntityNeutronCollector /* extends TileEntityBase10MultiBlockMachine */ {
    /*
     * WIP
     * @Override
     * public boolean checkStructure2(ChunkCoordinates aCoordinates, Entity aPlayer, IInventory aInventory) {
     * int tX = getOffsetXN(mFacing)-1, tY = yCoord, tZ = getOffsetZN(mFacing)-1;
     * }
     * static {
     * LH.add("reach.tooltip.multiblock.neutron.1",
     * "Two 3x3x3 with 1m inbetween made of the Block you crafting this of");
     * LH.add("reach.tooltip.multiblock.neutron.2", "a hollow 3x3x2 of Neutron Collector Parts inbetween");
     * LH.add("reach.tooltip.multiblock.neutron.3", "Main Block centered on Side-Bottom and facing outwards");
     * LH.add("reach.tooltip.multiblock.neutron.4", "Neutron Collector Parts are not accepting any Inputs or Outputs.");
     * }
     * @Override
     * public void addToolTips(List<String> aList, ItemStack aStack, boolean aF3_H) {
     * aList.add(Chat.CYAN + LH.get(LH.STRUCTURE) + ":");
     * aList.add(Chat.WHITE + LH.get("reach.tooltip.multiblock.neutron.1"));
     * aList.add(Chat.WHITE + LH.get("reach.tooltip.multiblock.neutron.2"));
     * aList.add(Chat.WHITE + LH.get("reach.tooltip.multiblock.neutron.3"));
     * aList.add(Chat.WHITE + LH.get("reach.tooltip.multiblock.neutron.4"));
     * super.addToolTips(aList, aStack, aF3_H);
     * }
     * @Override
     * public boolean isInsideStructure(int aX, int aY, int aZ) {
     * int tX = getOffsetXN(mFacing), tY = yCoord, tZ = getOffsetZN(mFacing);
     * return aX >= tX -1 && aY >= tY && aZ >= tZ - 1 && aX <= tX + 1 && aY <= tY + 2 && aZ <= tZ + 1;
     * }
     * @Override
     * public DelegatorTileEntity<TileEntity> getItemOutputTarget(byte aSide) {
     * return getAdjacentTileEntity(SIDE_BOTTOM);
     * }
     * @Override public DelegatorTileEntity<IInventory> getItemInputTarget(byte aSide) {return null;}
     * @Override public DelegatorTileEntity<IFluidHandler> getFluidInputTarget(byte aSide) {return null;}
     * @Override public String getTileEntityName() {return "reach.multitileentity.multiblock.neutron";}
     */
}
